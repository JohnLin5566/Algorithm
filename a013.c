/* A typical recursive implemtation of quick sort */
#include <stdio.h>
void swap( int *a, int *b){ int t = *a; *a = *b; *b = t;}
/* takes last element as pivot, places the pivot at its correct position in sorted array,
 * and places all elements smaller than pivot to left */
int partition( int arr[], int l, int h){

	int x = arr[h];
	int i = (l-1);

}
